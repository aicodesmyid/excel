# Excel

Rumus terbilang pada excel:

```
=LET(data1;$G$6;data2;TEXT(ABS(data1);"000000000000000");terbilang;IF(data1=0;"nol";IF(data1<0;"minus ";"")& SUBSTITUTE(TRIM(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(SUBSTITUTE( IF(--MID(data2;1;3)=0;"";MID(data2;1;1)&" ratus "&MID(data2;2;1)&" puluh "&MID(data2;3;1)&" trilyun ")& IF(--MID(data2;4;3)=0;"";MID(data2;4;1)&" ratus "&MID(data2;5;1)&" puluh "&MID(data2;6;1)&" milyar ")& IF(--MID(data2;7;3)=0;"";MID(data2;7;1)&" ratus "&MID(data2;8;1)&" puluh "&MID(data2;9;1)&" juta ")& IF(--MID(data2;10;3)=0;"";IF(--MID(data2;10;3)=1;"*";MID(data2;10;1)&" ratus "&MID(data2;11;1)&" puluh ")&MID(data2;12;1)&" ribu ")& IF(--MID(data2;13;3)=0;"";MID(data2;13;1)&" ratus "&MID(data2;14;1)&" puluh "&MID(data2;15;1));1;"satu");2;"dua");3;"tiga");4;"empat");5;"lima");6;"enam");7;"tujuh");8;"delapan");9;"sembilan");"0 ratus";"");"0 puluh";"");"satu puluh 0";"sepuluh");"satu puluh satu";"sebelas");"satu puluh dua";"dua belas");"satu puluh tiga";"tiga belas");"satu puluh empat";"empat belas");"satu puluh lima";"lima belas");"satu puluh enam";"enam belas");"satu puluh tujuh";"tujuh belas");"satu puluh delapan";"delapan belas");"satu puluh sembilan";"sembilan belas");"satu ratus";"seratus");"*satu ribu";"seribu");0;""));" ";" "));capitalize;UPPER(LEFT(terbilang;1))&RIGHT(terbilang;LEN(terbilang)-1);"(" & capitalize & " rupiah)")
```
